import "./Header.scss";

type Props = {};

const Header = (props: Props) => {
  return (
    <div>
      <h1 style={{textAlign: "center", padding: "2rem", fontSize: "3.2rem"}}>
        E-WALLET
      </h1>
    </div>
  );
};

export default Header;
